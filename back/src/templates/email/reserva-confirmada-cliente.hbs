<h2>¡Reserva Confirmada! 🎉</h2>

<p>Estimado/a <strong>{{cliente.nombre}}</strong>,</p>

<p>Nos complace informarle que su reserva ha sido confirmada exitosamente. A continuación encontrará todos los detalles:</p>

<div class="info-box">
    <h3>📅 Detalles de su Reserva</h3>
    <div class="info-row">
        <span class="info-label">ID Reserva:</span>
        <span class="info-value">#{{reserva.id}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Fecha:</span>
        <span class="info-value"><strong>{{reserva.fecha}}</strong></span>
    </div>
    <div class="info-row">
        <span class="info-label">Salón:</span>
        <span class="info-value">{{reserva.salon}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Dirección:</span>
        <span class="info-value">{{reserva.direccion}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Turno:</span>
        <span class="info-value">{{reserva.turno}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Temática:</span>
        <span class="info-value">{{reserva.tematica}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Importe Total:</span>
        <span class="info-value"><strong>{{reserva.importe_total}}</strong></span>
    </div>
</div>

{{#if servicios}}
<div class="info-box">
    <h3>🎁 Servicios Incluidos</h3>
    <ul>
        {{#each servicios}}
        <li>
            <strong>{{this.descripcion}}</strong> - 
            {{this.importe}} ARS
        </li>
        {{/each}}
    </ul>
</div>
{{/if}}

<p><strong>¡Estamos ansiosos de celebrar con ustedes!</strong></p>

<p>Si tiene alguna consulta, no dude en contactarnos.</p>

<p>Saludos cordiales,<br>
El equipo de PROGIII</p>

